package com.campusguard;

import org.junit.jupiter.api.Test;

/**
 * Placeholder so `mvn test` has something to run in P0.
 *
 * A real Spring context test needs a database; that arrives in P1 together with
 * Testcontainers. Deliberately not using @SpringBootTest here — it would fail
 * without Postgres running and make the first build check unreliable.
 */
class CampusGuardApplicationTests {

    @Test
    void placeholder() {
        // intentionally empty
    }
}
