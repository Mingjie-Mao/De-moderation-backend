<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks cleartext HTTP by default, which is the right default and the
  reason a local backend appears to be unreachable rather than insecure.

  The exemption is scoped to the emulator's alias for the host loopback and to
  localhost, so it cannot silently permit plaintext to anywhere else. A deployed
  backend is reached over TLS and needs nothing here.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
